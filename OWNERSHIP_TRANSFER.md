# Ownership Transfer Checklist

هذا الملف مخصص لتسليم المتجر كمشروع تجاري مكتمل إلى مالك جديد بدون اعتماد على المطور.

---

## 1) الملفات التي يجب تسليمها

1. **ملفات المشروع كاملة**
   - جميع ملفات الكود داخل المستودع
2. **قاعدة البيانات**
   - `data/store.db`
3. **ملف البيئة**
   - `.env` (أو نسخة منه بعد تحديث بيانات الدخول)
4. **دليل التشغيل**
   - `README.md`
   - `DEPLOYMENT.md`
   - `OWNERSHIP_TRANSFER.md`

---

## 2) قبل التسليم للمشتري

### أ) إعادة ضبط دخول الإدارة

```bash
npm run admin:reset -- --username=owner@buyer.com --password=BuyerStrongPass123!
```

### ب) التحقق من بيانات الدخول

- لوحة التحكم: `/admin.html`
- جرّب تسجيل الدخول بالبيانات الجديدة

### ج) تصدير نسخة احتياطية من البيانات

- من لوحة التحكم: تصدير نسخة JSON
- إضافة نسخة من ملف القاعدة `data/store.db`

---

## 3) حزمة التسليم المقترحة

يمكن تسليم مجلد مضغوط يحتوي:

- `project-files/` (الكود)
- `database/store.db`
- `env/.env`
- `docs/README.md`
- `docs/DEPLOYMENT.md`
- `docs/OWNERSHIP_TRANSFER.md`
- `credentials.txt` (معلومات دخول الإدارة)

---

## 4) بيانات الدخول الواجب تسليمها

- Admin URL: `https://your-domain.com/admin.html`
- Username: (القيمة المحدثة)
- Password: (القيمة المحدثة)
- Server access (إن لزم):
  - SSH / Hosting Panel
  - Git repository access

---

## 5) ماذا يملك المشتري بعد التسليم؟

- الكود المصدري الكامل
- قاعدة بيانات حقيقية بكل المنتجات والعملاء والطلبات
- حساب الإدارة الكامل
- القدرة على التشغيل والتطوير بدون أي اعتماد على المطور السابق

---

## 6) ملاحظة قانونية تشغيلية

بعد التسليم:

- يجب إزالة أي وصول للمطور السابق (SSH/Git/Hosting)
- يجب تغيير كلمات المرور (سيرفر، لوحة استضافة، البريد، بوابة الدفع)
- يوصى بإجراء Backup دوري لقاعدة البيانات
